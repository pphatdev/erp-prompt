# Task Context: HRM

## Objective
Stand up the backend API for the HRM module: workforce records, leave workflow, and payroll engine. Phases 4 (Recruitment/Performance) and 5 (QA & Privacy testing) remain.

## Checklist

### Phase 1 — Workforce
- [x] Migrations: `departments`, `positions`, `employees`
- [x] Models: `Employee`, `Department`, `Position` (UUID, `BelongsToTenant`, `Auditable`)
- [x] Resources, FormRequests, `EmployeeService`
- [x] Controllers: `EmployeeController`, `DepartmentController`, `PositionController`
- [x] Routes: `/api/v1/employees`, `/hrm/departments`, `/hrm/positions`
- [x] Field-level encryption for `base_salary` + `bank_name` / `bank_account_name` / `bank_account_number` (migration `2024_01_01_000020`, Laravel `'encrypted'` cast, account number masked except last 4 digits in `EmployeeResource`)
- [x] Employee Self-Service detail view: Implement `EmployeePolicy` and register it in `TenantServiceProvider` to allow employees to view their own profile details (`$user->employee?->id === $employee->id`) without administrative `hrm.employee.read` permission.

### Phase 2 — Leave
- [x] Migrations: `leave_types`, `leaves`
- [x] Models: `LeaveType`, `Leave` (now exposes `approvalRequests()` morphMany + `activeApprovalRequest()` helper)
- [x] Resources, FormRequests, `LeaveService` (balance calc + approve/reject)
- [x] Controllers: `LeaveController`, `LeaveTypeController`
- [x] Routes: `/api/v1/leaves`, `/leave-types`, `/employees/{employee}/leave-balance`
- [x] eApprovals integration — `LeaveService::submitRequest` opens an `ApprovalRequest` when a workflow with `module=hrm, type=leave` exists. `ApprovalService::notifyOriginModule` dispatches `ApprovalRequestFinalized`; HRM listener `SyncLeaveFromApproval` flips the Leave's status (with balance check). Legacy `/leaves/{id}/approve|reject` returns 422 when a workflow request is active so clients are pushed to `/approval-requests/{id}/process`.
- [x] `hrm.leave.*` policies — `LeavePolicy` + `LeaveTypePolicy` registered with `Gate` in `TenantServiceProvider`; controllers use `$this->authorize(...)` (base `Controller` now uses `AuthorizesRequests`). Owners can view/withdraw their own pending requests without `hrm.leave.delete`.
- [x] Leave withdrawal cancels its pending `ApprovalRequest` (status `cancelled`, history entry attributed to the actor) via `LeaveService::withdraw()`. `LeaveResource` exposes `approvalRequestId` + `approvalStatus`; index query eager-loads `approvalRequests` to avoid N+1. Postman: fixed `Process Approval Request` payload (`status` → `action`), added `Create Leave Approval Workflow`, and `Submit Leave Request` captures `approval_request_id`.

### Phase 2.5 - Holidays + Calendar (Shipped 2026-06-01)
- [x] Migration `2024_01_01_000092_create_holidays_table.php` - holidays with id, name, date, type (`public`/`company`/`optional`), `is_recurring` boolean for yearly anchors, notes. Unique on `(tenant_id, date, name)` so multiple holidays can share a date but each must be uniquely named. Index on date + type.
- [x] `Holiday` model with `BelongsToTenant` + `Auditable` + `SoftDeletes`; type constants, date + bool casts.
- [x] `HolidayService` - CRUD + `occurrencesInRange(from, to)` expanding recurring entries by year (clamps Feb 29 to Feb 28 on non-leap years), + `calendarFeed(from, to)` combining expanded holidays with approved/pending leaves so the month view renders mixed events in one round-trip.
- [x] `HolidayPolicy` registered. `HolidayController` + `HolidayResource` (camelCase). Routes: `apiResource('holidays')` + `GET /hrm/calendar?from=&to=`. Index filters: `?type`, `?search`, `?from`, `?to`, `?recurring_only`.
- [x] Perms `hrm.holiday.{read,write,delete}` seeded. Module slugs `hrm-timeoff-holidays` (`/hrm/timeoff/holidays`) and `hrm-timeoff-calendar` (`/hrm/timeoff/calendar`) seeded under `hrm-timeoff` (sort_order 5 + 6).
- [x] UI `pages/hrm/timeoff/holidays/index.vue` - KPI strip (Total, Public, Company, Optional), type chips, search, table with date+weekday, name, type badge, recurring chip, notes, edit/delete actions. New/Edit modal with name, date, type, recurring checkbox, notes.
- [x] UI `pages/hrm/timeoff/calendar/index.vue` - month grid (7 cols / 6 rows / 42 cells, anchored on the Sunday before the 1st). Prev / Today / Next month navigation watched to reload the feed. Legend chips for Public / Company / Optional / Leave. Each day cell shows day number + event count + colored event pills (holidays color-coded by type, leaves in warning). Click empty day to add holiday (gated by `hrm.holiday.write`), click holiday pill to edit, click leave pill is a no-op (leaves owned by the Leaves page). Today's cell ringed in primary, weekends faintly tinted, other-month cells dimmed.
- [x] Sidebar: Holidays + Calendar entries added under HRM > Time & Attendance. Breadcrumb map gains `holidays` + `calendar`.

### Phase 3 — Payroll
- [x] Migrations: `payroll_periods`, `payslips`
- [x] Models: `PayrollPeriod`, `Payslip`
- [x] Resources, FormRequest, `PayrollService` (compute earnings + tax/NSSF deductions)
- [x] Controllers: `PayrollPeriodController`, `PayslipController`
- [x] Routes: `/api/v1/hrm/payroll-periods`, `/hrm/payroll-periods/{id}/process|close`, `/payslips`
- [x] Postman collection updated (`docs/postman/erp_collection.json` → "HRM" folder, 24 requests + 7 new collection variables)
- [x] Queue `processPeriod()` for tenants with > 200 employees - `App\Jobs\ProcessPayrollPeriodJob implements ShouldQueue` (tries=1, timeout=600s); `QueueTenancyBootstrapper` (already registered in `config/tenancy.php`) carries the active tenant onto the worker. `PayrollService::shouldQueueProcessing()` returns true when `Employee::active()->count() >= QUEUE_THRESHOLD (200)`; new `queueProcessPeriod()` transitions period draft -> processing and dispatches the job. `PayrollPeriodController@process` branches on `shouldQueueProcessing()` - inline path returns the payslip collection, queue path returns 202 with `meta.queued=true`. Job validates `processing` status before working (skips silently if someone else moved it forward); rolls back to `draft` on exception so the user can retry. Workflow status seed gained a `processing` row (sequence 2) with allowed_next `[processed, draft]`; `draft` now lists `[processing, processed]` so both paths validate. Pest `PayrollQueueThresholdTest` (4 cases): small tenant -> sync 200 OK + no job pushed, partial-mocked large tenant -> 202 + `processing` + job pushed with correct period id, job skips when status != processing, job completes processing -> processed when status is processing.
- [x] FMS journal entry posting on `closePeriod()` — migration `2024_01_01_000021` adds `journal_entry_id` + `closed_at`; `PayrollService` aggregates gross/tax/nssf, derives net as the balancing figure, looks up four account codes from `config/hrm/payroll.php`, and posts a balanced journal via `AccountingService::postEntry()`. Missing codes ⇒ 422 listing what to create. Reference `PAYROLL-{periodId}` enforces idempotency. `PayrollPeriodResource` exposes `journalEntryId` + `closedAt`.
- [x] PDF payslip generator + ESS portal endpoint - `barryvdh/laravel-dompdf ^3.0` added to `composer.json` (run `composer update barryvdh/laravel-dompdf` to install). New routes: `GET /api/v1/me/payslips` (ESS portal, force-filters to caller's employee_id, 404 when no employee link, requires `hrm.payslip.read.self` OR `hrm.payroll.read`) and `GET /api/v1/payslips/{id}/pdf` (streams `application/pdf` via DomPDF, gated by `PayslipPolicy::view` so owners + `hrm.payroll.read` admins both work). Blade template at `resources/views/hrm/payslip-pdf.blade.php` renders earnings + deductions tables with tenant name in the header and `locale.currency` setting in totals. Pest `PayslipEssTest` covers 5 cases (me-payslips self-only filter, 404 on orphan user, PDF 403 for non-owner, PDF 200 + `%PDF-` magic header for owner, PDF 200 for admin on other employee).

### Configurable status flows (cross-cutting — shipped)
- [x] Migration `2024_01_01_000019_create_workflow_statuses_table.php`
- [x] `App\Models\Tenant\WorkflowStatus` model + `forModule` scope
- [x] `App\Tenants\Modules\IAM\Services\WorkflowStatusService` (`for` / `lookup` / `initialFor` / `validateTransition` / `flushCache`)
- [x] CRUD: `GET/POST/PUT/DELETE /api/v1/workflow-statuses` + `GET /workflow-statuses/modules`
- [x] Seeder `TenantDatabaseSeeder::seedWorkflowStatuses()` populates 6 modules (`hrm.application`, `hrm.leave`, `hrm.appraisal`, `hrm.vacancy`, `hrm.employee`, `hrm.payroll_period`)
- [x] Domain services refactored — `RecruitmentService`, `PerformanceService`, `LeaveService`, `PayrollService`, `EmployeeService` all consult the table
- [x] Removed `Application::STATUS_FLOW` + `Appraisal::STATUS_FLOW` constants — DB is authoritative
- [x] Postman "IAM" folder grew 5 → 11 with Workflow Status requests
- [x] Skills doc: `skills/hrm/rules.md` §2 "Status Flows" subsection
- [x] Frontend `/workflow-statuses` admin page - `frontend/pages/settings/workflow-statuses.vue` ships the Shop > All Products toolbar pattern (sticky pill-style module picker + "New status" primary action) + status-card grid (badge-soft tint by row.color, initial/terminal/transition chips, allowed-next mono pills). Create/edit modal exposes module/key/label/color/icon/sequence/initial/terminal + "Allowed next" toggle chips sourced from sibling statuses in the same module. Module and key locked on edit (would break referential validate). Client-side guards: a status can't be both initial+terminal, terminal statuses can't have outgoing transitions; backend FormRequest enforces the same plus regex `[a-z0-9_]+`. `useWorkflowStatuses` composable handles camelCase->snake_case payload translation at the boundary. Sidebar entry added under Configurations (gated on `iam.workflow_statuses.{read,write}`), breadcrumb slug seeded. `iam.workflow_statuses.{read,write,delete}` permissions added to TenantDatabaseSeeder. ModuleSeeder gained `settings-workflow-statuses` slug.

### Public Careers surface (cross-cutting — shipped)
- [x] `App\Tenants\Modules\HRM\Controllers\PublicCareersController` — `GET /api/v1/public/job-vacancies` (filtered to `status=open`), `GET /api/v1/public/job-vacancies/{id}` (404s anything non-open), `POST /api/v1/public/applications` (rejects closed/draft vacancies with 422)
- [x] Routes live OUTSIDE `auth:api`; tenant still resolves through `X-Tenant-Handle` (InitializeTenancyByHandle)
- [x] Postman "Recruitment" folder reordered: candidate flow (List Public Vacancies → Get Public Vacancy → Public: Submit Application) first, admin paths after. New `public_job_vacancy_id` collection variable.

### Phase 4 — Recruitment & Performance (MVP shipped)
- [x] Migrations: `job_vacancies`, `applications`, `appraisals`
- [x] Models: `JobVacancy`, `Application` (with `STATUS_FLOW` map), `Appraisal` (with lifecycle map)
- [x] Resources, FormRequests, `RecruitmentService` + `PerformanceService`
- [x] Controllers: `JobVacancyController`, `ApplicationController`, `AppraisalController`
- [x] Routes: `/api/v1/job-vacancies` (+ publish/close), `/applications` (+ PATCH /status), `/appraisals` (+ submit/review/close)
- [x] Postman: "Recruitment" (12 requests) + "Performance" (8 requests) sub-folders
- [ ] Interview scheduling / structured feedback (deferred — not in MVP scope)
- [x] 360-degree peer feedback + OKR tracking - migration `2024_01_01_000098_create_appraisal_peer_feedbacks_table.php` (appraisal_id + reviewer_id + rating(3,1) + strengths/concerns/notes + status FSM invited|submitted|declined + invited_at/submitted_at + unique(appraisal_id, reviewer_id)). `AppraisalPeerFeedback` model with `BelongsToTenant + Auditable + SoftDeletes`. `Appraisal` model gained `peerFeedbacks() HasMany`, `okrProgress(): float` (weighted aggregation across goal-level + KR-level weights, defaults to equal weight when unset), `averagePeerRating(): ?float`. `PerformanceService` extended: `invitePeerReviewer` (idempotent, blocks self-review), `submitPeerFeedback` (upsert per reviewer, blocks reviewed/closed appraisals), `aggregatePeerFeedback`, `computeFinalScoreWithPeer` (reallocates peer weight from manager so trio sums to 100, falls back to self/manager when no peer rating). `applyWeightedRating` now auto-blends peer average when `hrm.appraisal.peer_evaluation_weight` is set. `AppraisalPeerFeedbackController` (index + invite + submit) at `/api/v1/appraisals/{appraisal}/peer-feedback/*`. `AppraisalPolicy::inviteReviewer` allows the assigned manager or `hrm.performance.peer_review` / `hrm.performance.write` holders. New permission `hrm.performance.peer_review` seeded. Pest `PeerFeedbackTest` (7 cases): invite idempotency, self-review block, submit flips status + stamps submitted_at, aggregate ignores invited rows, blends peer into weighted rating, falls back to 2-component when peer setting absent, HTTP 403 on submit-as-other without admin grant.
- [x] `hrm.recruitment.*` / `hrm.performance.*` Eloquent Policies — `JobVacancyPolicy`, `ApplicationPolicy`, `AppraisalPolicy` registered in `TenantServiceProvider`. Controllers gate every action via `$this->authorize(...)`; FormRequests now `return true` from `authorize()`. AppraisalPolicy short-circuits for the reviewee (view/submit) and assigned reviewer (view/review).

### Phase 5 — QA & Privacy
- [x] P0: tenant isolation tests (Pest) — `tests/Feature/Tenant/HRM/HrmTenancyIsolationTest.php` boots Tenant A + Tenant B, seeds employee / leave / leave_type / payroll_period / payslip on A, asserts Tenant B's connection sees 0 of each and `find()` returns null. Standalone `TestCase` (not `TenantTestCase`) so two `tenancy()->initialize()` calls can sit side-by-side.
- [x] P0: privacy — `tests/Feature/Tenant/HRM/HrmPrivacyTest.php` covers 6 cases over a self-service `employee`-role user linked to one of two seeded employees: (a) GET /employees/other → 403 via EmployeePolicy::view; (b) GET /employees/self → 200 with baseSalary/bankName/bankAccountName/bankAccountNumber all null (no `hrm.payroll.read`); (c) admin → real values + last-4 account mask; (d) GET /payslips returns only self rows; (e) ?employeeId=other filter is dropped server-side; (f) GET /payslips/other → 403; (g) GET /payslips/self → 200.
- [x] P1: payroll accuracy fixtures — `tests/Feature/Tenant/HRM/PayrollAccuracyTest.php` data-provider locks `PayrollService::computeFor()` math (10% flat tax + 4% NSSF) against 7 salary brackets (1000 → 12345.67, plus 2 odd-cent cases verifying rounding). Separate cases cover `monthlyWorkHoursStandard()` setting honor (zero / non-numeric / valid) and `defaultPayday()` clamping to 1-31.
- [x] Audit log assertions — `tests/Feature/Tenant/HRM/HrmAuditLogTest.php` uses `Log::spy()` (the `Auditable` trait writes to `Log::info` — no audit_logs DB table yet) and asserts the expected `Audit Log: {action}` + model+id pair was logged at least once on: hire (`EmployeeService::createEmployee`), terminate (`terminateEmployee` -> update + delete), leave approve (`LeaveService::approve`), and payroll process (`PayrollService::processPeriod` -> period update + per-payslip create).

### Phase 6 — Candidate Login & Quiz Assessment (Backend MVP shipped)
- [x] Migration `2024_01_01_000022_create_quiz_assessment_tables.php` — `quizzes`, `quiz_questions` (Laravel-`encrypted` `correct_answer`), `quiz_attempts` (SHA-256 `secure_token_hash`, expires_at, answers JSON, score, passed)
- [x] Models: `Quiz`, `QuizQuestion`, `QuizAttempt` (token hash hidden via `$hidden`)
- [x] `QuizService` — quiz authoring, magic-link issuance (raw token returned once, only its hash stored), idempotent start, auto-grading (`single_choice` / `multiple_choice` / `short_text`), application transition to `assessment_completed`
- [x] Admin `QuizController` — `apiResource('quizzes')`, `POST /quizzes/{id}/questions`, `POST /applications/{id}/quiz-attempts` (returns `{ attempt, token, candidateUrl }`)
- [x] Candidate-facing `CandidateQuizController` — public routes `/candidate/auth`, `/candidate/quizzes/{attempt}/start|submit` (token via query string, no Passport). Sanitised quiz payload omits `correct_answer`.
- [x] `QuizPolicy` registered with `Gate`; `hrm.quiz.read|write|delete`
- [x] Seeder: `hrm.application` workflow extended with `assessment` + `assessment_completed`; new `hrm.quiz_attempt` module (`invited → in_progress → completed | expired | abandoned`)
- [x] `TransitionApplicationRequest` no longer hardcodes the enum — `workflow_statuses` is authoritative
- [x] Postman: new HRM › Quiz Assessment subfolder (10 requests), `quiz_id` / `quiz_attempt_id` / `candidate_quiz_token` / `vehicle_id` collection variables added
- [x] Frontend Portal: Candidate Sandboxed Assessment dashboard `/candidate/quiz?token=...&handle=...` shipped. Standalone `frontend/layouts/candidate.vue` (no admin chrome, no Pinia auth). `frontend/composables/useCandidateQuiz.ts` is a fetch wrapper independent of `useApi` — pulls the tenant handle from the URL query param so the page works without a tenant store, and authenticates via `?token=` only. `frontend/pages/candidate/quiz.vue` walks five phases (loading -> landing -> in_progress -> result | error). Landing card surfaces title + description + question count + time limit + expiry. In-progress view: question navigator pills (gray / answered green / active primary), live timer chip with warning + danger colors at <=5min / <=1min, single-choice radio / multiple-choice checkbox / short-text textarea, prev/next nav, "Review & submit" confirmation modal showing unanswered count. Result view: score percent + pass/submitted badge, friendly "recruiter will follow up" copy. Auto-submit on timer expiry. Resume-in-progress: if attempt status is already `in_progress` on auth, skip landing and start timer immediately. Status-specific error states for `completed` / `expired` / `abandoned` so a candidate returning to an old link gets a clear message. `QuizController@assignToApplication` (backend) now bakes `&handle=` into the generated `candidateUrl` so the link the candidate receives is self-contained.

### Phase 7 — Advanced Interviewing & Panel Feedback (Backend MVP shipped)
- [x] Migration `2024_01_01_000023_create_interview_tables.php` — `interviews`, `interview_feedback` (unique `(interview_id, interviewer_id)`), `interview_interviewer` M:N pivot
- [x] Models: `Interview` (HasMany feedback, BelongsToMany interviewers, `averageRating()` accessor), `InterviewFeedback`
- [x] `InterviewService` — schedule (best-effort moves Application → `interview`), reschedule (only when scheduled), cancel (workflow-validated), complete, `submitFeedback` (upsert by interviewer), `scorecardFor()` aggregate (`averageRating`, recommendations tally, submitted/pending counts)
- [x] `CalendarSyncService` — RFC 5545 (.ics) invite builder including all interviewers + candidate as ATTENDEEs. Real Google/Outlook OAuth deferred.
- [x] `InterviewController` — apiResource + `/cancel`, `/complete`, `/feedback`, `/scorecard`, `/invite.ics`
- [x] `InterviewPolicy` registered with `Gate`; assigned-interviewer short-circuit on `view` + `submitFeedback`
- [x] Seeder: new `hrm.interview` workflow_statuses module (`scheduled → completed | cancelled | no_show`)
- [x] Postman: HRM › Interviewing subfolder (9 requests), `interview_id` collection variable added

### Phase 8 — Digital Offer & Onboarding Pipeline (Workforce Transition)
- [x] Migrations: `offers`, `onboarding_checklists`, `onboarding_tasks` — migration `2024_01_01_000096_create_offer_and_onboarding_tables.php`. offers carries application_id FK + nullable employee_id + reference_number (unique per tenant) + encrypted base_salary/signing_bonus (text) + currency/probation + status FSM + esign_provider/envelope_id/payload (jsonb) + sent_at/signed_at/declined_at + decline_reason. onboarding_checklists carries offer_id FK + employee_id + name + status + counters (total_tasks/completed_tasks) + target_completion_date. onboarding_tasks carries checklist_id FK + title/description + owner_role + owner_user_id + due_offset_days (signed for pre-arrival) + due_date + status + sort_order + completion meta. All three tables BelongsToTenant + SoftDeletes + composite indices.
- [x] Models: `Offer` (EncryptedWithFallback on base_salary + signing_bonus, status constants + isTerminal()), `OnboardingChecklist` (status constants + progressPercent() accessor + tasks() ordered by sort_order), `OnboardingTask` (status + role constants, ownerUser/completedBy relations). `Application` model gained `offers() HasMany` so the candidate timeline can traverse them.
- [x] Services: `OfferService` (createOffer with OFR-YYYYMM-NNN reference + settings-driven prefix, sendOffer via ESignatureService::createEnvelope, markAccepted that runs convertToEmployee + seedDefaultChecklist atomically and is idempotent, markDeclined with reason, expireStaleOffers sweep). `OnboardingService::seedDefaultChecklist` materializes 11 stock tasks across hr / it / finance / manager / facilities owners with offsets -7..+30 days. `transitionTaskStatus` validates via WorkflowStatusService, completeTask is the convenience wrapper, refreshChecklistProgress rolls counters + status up to checklist. `ESignatureService` ships provider-agnostic createEnvelope(mock/docusign) + handleWebhookPayload + verifySignature; docusign branch throws until tenant creds are configured.
- [x] Hand-off: Automatic `Employee` creation trigger on digital sign acceptance of offer letter — `OfferService::markAccepted` calls `RecruitmentService::convertToEmployee` inside one DB transaction so accepting the offer atomically mints the Employee, links `offers.employee_id`, and seeds the default onboarding checklist via `OnboardingService::seedDefaultChecklist`. Idempotent: second call returns the same Employee + checklist.
- [x] Schema update: Add `employee_id` (nullable foreign key) to `applications` table to link hired candidates to their employee profiles
- [x] History linkage: Update `Employee` model and services to link and traverse pre-hire details (applications, quiz assessments, interviews, panel feedback, offer letters) in the employee's timeline/profile
- [x] Workforce listing: Assert that the newly provisioned employee record defaults to the initial `active` status so they are instantly searchable and displayed in the global employee directory listing (`GET /employees`)
- [x] API surface: `OfferController` (apiResource + `/send` + `/accept` + `/decline` + public `/offers/sign-webhook` outside auth:api), `OnboardingTaskController` (`/onboarding-checklists`, `/onboarding-checklists/{id}`, `/onboarding-tasks`, `PATCH /onboarding-tasks/{id}/status`). camelCase `OfferResource` / `OnboardingChecklistResource` / `OnboardingTaskResource`. `StoreOfferRequest` + `UpdateOfferRequest` translate camelCase JSON into snake_case payloads. `OfferPolicy` / `OnboardingChecklistPolicy` / `OnboardingTaskPolicy` registered in `TenantServiceProvider`. New permissions `hrm.recruitment.offer` + `hrm.recruitment.onboarding` seeded.
- [x] Seeders + workflow statuses: `TenantDatabaseSeeder::seedWorkflowStatuses()` now seeds `hrm.offer` (draft -> sent -> accepted | declined | expired) and `hrm.onboarding_task` (pending -> in_progress -> completed | skipped). `SettingService::defaults()` ships `numbering.offer_reference_prefix=OFR-`. `ModuleSeeder` gains `hrm-offers` (`/hrm/offers`) + `hrm-onboarding` (`/hrm/onboarding`) under parent `hrm`.
- [x] Pest `OfferLifecycleTest` covers 7 cases: createOffer stamps reference + draft, sendOffer flips to sent + envelope id, markAccepted converts Application -> Employee + seeds 11-task checklist with correct due dates, accept idempotency (no duplicate Employee/checklist on webhook retry), markDeclined records reason without creating Employee, expireStaleOffers sweep, completeTask bumps checklist counters + flips status to in_progress.
- [x] **Frontend UI: Job Offers List Workspace (`/hrm/offers`)** — `frontend/pages/hrm/offers/index.vue` ships a status-chip filter (draft/sent/accepted/declined/expired), candidate-name search, KPI strip (total + per-status counts), and a table with candidate name, reference number, title, effective/expires dates, base salary (gated by `hrm.payroll.read` computed), status badge, and an action column. Actions: open candidate offer tab (NuxtLink to `/hrm/recruitments/candidates/{appId}#offer`), delete draft (modal), mark accepted (modal explaining the convertToEmployee + checklist side-effects), decline (reason textarea modal). `expiresAt` highlights warning color when ≤3 days away on a sent offer. `useOffers` composable wraps `GET/POST/PATCH/DELETE /offers` + `/send` + `/accept` + `/decline`.
- [x] **Frontend UI: Onboarding Workspace (`/hrm/onboarding`)** — `frontend/pages/hrm/onboarding/index.vue` is a master/detail layout. Left rail lists checklists with progress bar (color-tinted by completion %), checklist status badge, target date, and assigned employee name. Right pane shows selected checklist KPIs + tasks grouped by owner role (HR / IT / Finance / Manager / Facilities / Other) using `OWNER_ROLE_META` icons. Task cards expose a checkbox button that calls `PATCH /onboarding-tasks/{id}/status` with `completed`, plus a separate skip button. Status filter chips at top, pagination on the list. Counters refresh from the server after each transition so progress stays accurate. `useOnboarding` composable wraps `listChecklists` + `showChecklist` + `transitionTask`.
- [x] **Frontend UI: Candidate Profile Offer Tab Integration (`/hrm/recruitments/candidates/[id].vue`)** — added a `glass-card` tab bar (Overview / Offer & Onboarding) above the existing bento grid. Offer tab gated by `hrm.recruitment.offer`. URL hash `#offer` auto-selects the offer tab so the deep link from `/hrm/offers` works. State branches: (a) candidate not yet `hired` → info card telling the user to advance the stage first; (b) hired without offer → "Create Offer" card opening a form modal (title, effective/expires, base salary + signing bonus gated by `hrm.payroll.read`, currency, probationMonths default 3, notes); (c) draft → details card + Edit / Delete / Send Offer actions (Send opens a provider picker with Mock / DocuSign tiles); (d) sent → sent metadata (sentAt, provider, envelopeId) + Accept manually (wet-ink confirm modal explaining the convert + seed) + Decline (reason textarea); (e) accepted → signed badge + "View Employee" link + live onboarding checklist card with overall progress bar + per-owner-role tile counts (done/total); (f) declined/expired → terminal metadata + "Draft new offer" reset button. Modals live at NuxtLayout root so they overlay correctly.

### Fixed Asset Custody Integration (Assets tab)
- [x] Model relationship: `Employee::assets()` HasMany on `App\Models\Tenant\Asset` keyed by `custodian_employee_id`.
- [x] Response serialization: `EmployeeResource` ships `assets` via `whenLoaded` closure using `App\Tenants\Modules\Assets\Resources\AssetResource::collection` (closure form, not `MissingValue` placeholder).
- [x] API Eager Loading: `EmployeeController@show` and `@me` now load `['department', 'position', 'assets']` so the resource emits the array on the detail + self-service endpoints.
- [x] Profile Assets UI Tab: `frontend/pages/hrm/employees/[id].vue` gained the **Assets** tab — table with asset code, name (+ serial), category, condition, status, and net book value. Row click navigates to `/assets?highlight={id}`. Tab gated by `assets.tracking.read` OR own-profile (email match against authenticated user). `Badge` variants drive status/condition coloring (in_use/active/assigned -> success; maintenance/repair -> warning; disposed/lost/stolen -> danger; available/idle -> info; condition new/excellent -> success, good -> info, fair -> warning, poor/damaged/broken -> danger).

### Phase 9 — Tenant-Configurable HRM Settings
- [x] Document and define settings rules: Declare exhaustive registry for HRM settings (probation, revert conversion window, standard work week, accrual cycle, negative balance, geofencing, IP whitelisting, monthly work hours standard, default payday, FMS posting, appraisal weights) in `skills/hrm/rules.md` § 10 and `skills/hrm/skill.md` § 8. **Mandated strict tenant-admin gates (`settings.read`/`settings.write` permissions)** and **strict database connection isolation** (ensuring changes only affect the active self-tenant environment with zero cross-tenant bleeding).
- [x] Backend: Declare settings default registry inside `SettingService::defaults()` for the `hrm` group. 21 keys across 5 sub-groups (Recruitment / Leave / Attendance / Payroll / Performance) seeded with the defaults from `skills/hrm/rules.md` §10 registry. Auto-populated on first `SettingService::all()` for new tenants via existing `ensureDefaults()` path.
- [x] Security Gate Audit: `SettingController@index` now enforces `settings.read` via `$request->user()->hasPermission('settings.read')` (throws `AuthorizationException` -> 403); write/update was already gated by `UpdateSettingsRequest::authorize()` checking `settings.write`. Public `/settings/public` left open (login branding). Pest `HrmSettingsSecurityTest` covers 6 cases (employee 403 on read/read?group=hrm/write, admin 200 on read/write, public endpoint stays open for unauthenticated branding).
- [x] Multi-Tenant Isolation Audit: Pest `HrmSettingsCrossTenantTest` boots two tenants, writes `hrm.payroll.default_payday` (and two other hrm.* keys) on Tenant A, verifies Tenant B cold-boots to the catalogue defaults via `ensureDefaults()`, then writes on Tenant B and confirms Tenant A's previously-stored value is preserved. Second test asserts written `tenant_settings.tenant_id` matches the active connection's tenant handle for both tenants.
- [x] Backend: Refactor `LeaveService` to retrieve and enforce standard work week and negative balance setting validation rules. New `countWorkingDays(start, end)` reads `hrm.leave.standard_work_week` (defensive normalization, fallback Mon-Fri); `submitRequest()` honors `hrm.leave.allow_negative_balance` — bypasses the balance throw when true, used for emergency/unpaid flows. Half-day session still locked at 0.5d. Pest: `LeaveSettingsTest` covers 6 cases (default Mon-Fri span, weekend-only zero, Sun-Thu override, garbage-value fallback, default-throws, allow-negative-bypasses). Constructor DI added for `SettingService`.
- [x] Backend: Refactor `AttendanceController` and geofencing calculations to enforce coordinate radius, IP whitelisting, and auto clock-out settings. `AttendanceService` now consults `hrm.attendance.*` settings: `enforceGeofence` gates on `enable_geofencing` (default OFF — opt-in), falls back from per-department `geofence_radius_meters` → tenant `geofence_radius_meters` → 100m. `enforceIpWhitelist` gates on `enable_ip_whitelisting`, unions tenant `ip_whitelist` (comma-separated) with the legacy per-department list, fails closed when both are empty. New `autoCloseOpenSessions(asOf)` closes open clock-ins older than `auto_clock_out_hours` (default 12) at `check_in + window`; hooked into `reconcileAll()` and surfaced in the controller response. Constructor DI for `SettingService`. Pest: `AttendanceSettingsTest` covers 9 cases (geofence on/off/fallback/over-radius, IP on/off/tenant-only/fail-closed, auto-close stale/fresh).
- [x] Backend: Refactor `PayrollService` to dynamically load work hours standard, monthly default payday, FMS posting toggle, and FMS ledger account mappings from the `hrm.payroll.*` settings. New public accessors `monthlyWorkHoursStandard()` (clamps to ≥1, fallback 160), `defaultPayday()` (clamps to 1-31, fallback 25), `fmsPostingEnabled()` (fallback true so historic behavior is preserved). `computeFor()` overtime divisor now reads the setting. `closePeriod()` short-circuits the journal post when `hrm.payroll.fms_posting_enabled` is false. `resolvePayrollAccounts()` per-account-code lookup chain: tenant setting → `config('payroll.accounts')` (env-driven) → empty (surfaces in the missing-codes error). Constructor DI for `SettingService`. Error message updated to point at the new settings. Pest: `PayrollSettingsTest` covers 8 cases (monthly hours valid / zero / non-numeric fallback, payday in-range / out-of-range, FMS toggle default-true + false-disables, closePeriod-skip-journal, tenant account-code override surfaces in missing-codes message).
- [x] Backend: Refactor `PerformanceService` to read and compute appraisal scores using dynamic self-appraisal and manager-appraisal weighting settings. New public `appraisalWeights()` reads `hrm.appraisal.self_evaluation_weight` + `hrm.appraisal.manager_evaluation_weight`, validates sum=100 + range 0..100 + numeric, and logs `Log::warning` + falls back to [20, 80] on misconfiguration (keeps the appraisal pipeline live instead of throwing mid-close). New `computeFinalScore(self, manager): float` (rounded 2dp) and `applyWeightedRating(Appraisal, self, manager): float` that persists into `overall_rating`. Constructor DI for `SettingService`. Pest: `PerformanceSettingsTest` covers 9 cases (defaults, valid pair, sum!=100 fallback, non-numeric fallback, out-of-range fallback, 3 score-compute cases at 20/80, 50/50, 100/0, persist into overall_rating).
- [x] Backend: `Setting::GROUPS` constant now includes `'hrm'` (plus the other group keys already in the defaults registry: `pos`, `calendar`, `ecommerce`, `fms`).
- [x] Backend: `UpdateSettingsRequest::withValidator()` now branches on every `hrm.*` key in the registry. New helpers: `assertBool` (true/false/1/0/'1'/'0'/'true'/'false'), `assertIntInRange` (probation 0-120, revert window 0-365, geofence radius 1-100000, auto-clock 1-72, work hours 1-744, payday 1-31), `assertNumericInRange` (carryover 0-365, appraisal weights 0-100), `assertAccountCode` (`/^[A-Za-z0-9_-]{1,40}$/`, non-empty, trimmed), `assertWorkWeekArray` (non-empty, dedupe, members 1-7). Cross-key `validateAppraisalWeightsPair` enforces self + manager weights summing to 100 (sources the missing side from `SettingService` when only one key is in the payload; skipped on per-key error). `accrual_cycle_start` is a string enum (`LEAVE_ACCRUAL_CYCLES`). Numbering rules unchanged.
- [x] Frontend: consolidated `/settings/apps/hrm/index.vue` page with 7 tabs driven by `?tab=` query (Leave Types / Prefix Code / Recruitment / Leave / Attendance / Payroll / Performance). Settings tabs share `useHrmSettings()` composable — loaded lazily on first activation, save/dirty/alert UI lives in the parent header. Leave Types tab uses `LeaveTypesSection.vue` (CRUD with own state); Prefix Code tab embeds existing `PrefixCodeMatrix` + `usePrefixCodes(['hrm'])`. Five settings tabs use the existing `*Section.vue` components (`RecruitmentSection`, `LeaveSection`, `AttendanceSection`, `PayrollSection`, `PerformanceSection`). Browser back/forward syncs via `watch(route.query.tab)`. Sidebar in `layouts/default.vue` collapsed to a single "Human Resource" entry pointing to `/settings/apps/hrm`. Standalone pages `leave-types.vue`, `prefix-code.vue`, `recruitment.vue`, `leave.vue`, `attendance.vue`, `payroll.vue`, `performance.vue` deleted.

### Phase 10 — Hierarchical Working Days/Hours Setting
- [x] Database Migration: `2024_01_01_000097_create_work_schedules_table.php`. `work_schedules` carries `target_type` (global/department/employee), nullable `target_id` (UUID, validated by service since the column points at different tables), `day_of_week` (1-7 ISO), `is_work_day` (bool), `intervals` (jsonb array of `{start, end}` HH:MM strings). Unique on `(tenant_id, target_type, target_id, day_of_week)` so one target can't carry two rows for the same weekday. SoftDeletes preserved for audit.
- [x] Backend Model: `App\Models\Tenant\WorkSchedule` with `BelongsToTenant + Auditable + SoftDeletes`. Target constants (`TARGET_GLOBAL`/`DEPARTMENT`/`EMPLOYEE` + `TARGET_TYPES` + `PRECEDENCE` for resolver). `totalMinutes()` / `totalHours()` accessors sum intervals. `parseTime('HH:MM')` -> minutes-from-midnight helper.
- [x] Backend Seeder: `TenantDatabaseSeeder::seedDefaultWorkSchedule()` (called after `seedWorkflowStatuses()`) idempotently upserts 7 global rows: Mon-Fri full day (`08:00-12:00 + 13:00-17:00`), Sat half-day (`08:00-12:00`), Sun off.
- [x] Backend Resolver: `App\Tenants\Modules\HRM\Services\WorkScheduleService`. `resolveFor(date, employee?)` walks Employee -> Department -> Global -> legacy `hrm.leave.standard_work_week` fallback and returns `{is_work_day, intervals, source, schedule}`. `source` is `employee | department | global | default` so callers can surface which layer drove the answer. Per-request memo on `rowFor()` so a 30-day leave-duration count doesn't re-issue 30 queries. `countWorkingDays(start, end, employee?)`, `isWorkDay(date, employee?)`, `snapshotFor(targetType, targetId)` (7-row collection padded with off-placeholders for the editor), `upsertWeek(targetType, targetId, days[])` (atomic week replace with target existence + interval validity checks), `clearTarget(targetType, targetId)` (rejects global).
- [x] Backend Integration: `LeaveService::countWorkingDays(start, end, ?employeeId)` now delegates to `WorkScheduleService::countWorkingDays()`. Constructor DI added for the resolver. Dead `DEFAULT_WORK_WEEK` constant + `normalizeWorkWeek()` helper removed from `LeaveService` (legacy fallback lives in the resolver). `submitRequest()` passes the employee_id through so per-employee overrides apply.
- [x] Backend Integration: `AttendanceService::classifyDate(employee, date)` now consults `WorkScheduleService::isWorkDay($date, $employee)` instead of `CarbonImmutable::isWeekend()`, so the reconciler honors department + employee overrides (e.g. branch on Sun-Thu). Constructor DI added.
- [x] Backend Controllers & Routes: `WorkScheduleController` with `snapshot` (7-row padded view), `index` (existing rows only), `upsert` (PUT bulk week replace), `destroy` (DELETE clear overrides, rejects global). Routes under `/api/v1/work-schedules`. `WorkScheduleResource` camelCase. New permissions `hrm.work_schedule.read` + `hrm.work_schedule.write` seeded; `WorkSchedulePolicy` accepts those OR `settings.read`/`settings.write` and is registered in `TenantServiceProvider`.
- [x] Backend Verification: `tests/Feature/Tenant/HRM/WorkScheduleTest.php` covers 9 cases — default global seed shape (Mon-Fri 480m / Sat 240m / Sun 0m), resolver returns global on no overrides, department override beats global (Fri off + Sun on for a Sun-Thu branch), employee override beats both (part-time Mon/Wed/Fri), `countWorkingDays` integration with `LeaveService` (per-employee + null-employee), `upsertWeek` replaces existing rows atomically (no dupes on re-upsert), `clearTarget` removes overrides and falls back to parent, `clearTarget(global)` throws.
- [x] Frontend UI: `frontend/components/hrm-settings/WorkSchedulesSection.vue` + `frontend/composables/useWorkSchedules.ts`. Scope picker (Global / Department / Employee) loads departments / employees lazily. Week editor renders 7 rows with day name + Work Day toggle + intervals (multiple per day, `Add interval` button, `time` inputs, total hours summed per day). Save submits the full week via `PUT /work-schedules`; Revert reloads the snapshot; "Clear overrides" calls `DELETE` to fall back to parent layer. Validation errors surface from the FormRequest (regex-checked HH:MM intervals).
- [x] Frontend Navigation: Added as the 8th tab in the consolidated `/settings/apps/hrm` page (key `work-schedules`, icon `ti-calendar-time`). Deep-link works via `?tab=work-schedules`. No new sidebar entry — sits under the single "Human Resource" rail item.

### Phase 8.5 — Recruitment flow restructure (resolves Offer/Appointment conflict)

**Why**: Phase 8 shipped offer-acceptance as the trigger for `convertToEmployee` + checklist seeding, which collides with the existing eApprovals appointment-request flow (two paths creating the same Employee). The agreed canonical flow is:

```
Applied → Screening → Shortlisted
  → Interview / Technical Assignment / Final Interview (optional, settings-driven — Phase 11)
  → Job Offer       (← offer letter drafted HERE)
  → Hired           (auto on offer accept; HR submits Appointment Request here)
  → Onboarding      (auto on Appointment Request approval; Employee + checklist materialise here)
```

- [x] Seeder: `TenantDatabaseSeeder::seedWorkflowStatuses` for `hrm.application` extended — inserted `final_interview` (sequence 7) and `onboarding` (sequence 10, `is_terminal=true`); `hired` now `is_terminal=false` with `allowed_next=['onboarding', 'rejected', 'withdrawn']`; renumbered; `interview.allowed_next` includes `final_interview`. Idempotent re-seed.
- [x] Backend: `OfferService::createOffer` — status gate is now `'offer'`. `DomainException` message rewritten.
- [x] Backend: `OfferService::markAccepted` — `convertToEmployee` + `seedDefaultChecklist` calls removed. New behavior: flip offer to `accepted`, transition Application from `offer` → `hired` via `WorkflowStatusService`. Constructor no longer needs RecruitmentService/OnboardingService; class docblock updated.
- [x] Backend: `SyncEmployeeAppointmentFromApproval::handle` — after `convertToEmployee`, looks up the application's most recent accepted Offer, links `offers.employee_id`, seeds onboarding checklist, then validates + applies `hired` → `onboarding` Application transition. Wrapped in the same DB::transaction. DomainException still logged.
- [x] Backend tests: `OfferLifecycleTest` rewritten — setUp uses `status='offer'`; new `test_create_offer_throws_when_application_is_not_at_offer_stage`; accept-path tests now assert no Employee/checklist; idempotency case asserts no double-transition; decline case asserts `status` stays `offer`. Added new `AppointmentApprovalConversionTest` (approval → convertToEmployee + seedDefaultChecklist + `status='onboarding'` + offer.employee_id linked; rejection path; swallowed-DomainException path).
- [x] Frontend: candidate page (`pages/hrm/recruitments/candidates/[id].vue`) — `STATUS_FLOW` / `STAGE_INDEX` extended with `assessment`, `assessment_completed`, `final_interview`, `onboarding`. Manual advancement caps at `offer` (`STATUS_FLOW.offer = STATUS_FLOW.hired = []` aside from rejected/withdrawn). `canDraftFromStage = status === 'offer'`. `draftOfferShortcut` no longer auto-advances. Accepted-state branch surfaces "Submit Appointment Request" at `hired`, pending-request link if already submitted, "View Employee" at `onboarding`. Auto-open Offer tab broadened to fire at `offer | hired | onboarding`.
- [x] Frontend: in the offer tab a new branch (`!offer && status === 'hired'`) handles legacy/admin paths where the candidate reached `hired` without an offer letter — surfaces the appointment-request CTA directly.
- [x] Frontend: `layouts/default.vue` `SLUG_LABELS` — `final_interview` and `onboarding` added.
- [x] Docs sync: `skills/hrm/rules.md` §13 rewritten; `skills/hrm/recruitment/flow.md` Stage 5 + Mermaid + text-flowchart rewritten; `skills/hrm/employee_data_collection.md` recruitment note clarified; `skills/hrm/testing.md` §4 added.

### Phase 11 — Recruitment Candidate Pipeline Settings
- [ ] Database Schema: Assert or extend the `workflow_statuses` table's JSONB `metadata` column for Candidate Kanban configurations (sorting, visibility, fields to display, conversion eligibility).
- [ ] Backend Model & Service: Update `WorkflowStatus` and `WorkflowStatusService` to support:
  - Validation to ensure only one stage has `is_initial = true` per module.
  - Deletion safety check blocking status drops when active applications remain in that status.
  - Status code/key generation slug logic.
- [ ] Backend API: Add or extend `WorkflowStatusController` endpoints to allow settings administrators to:
  - Add a custom candidate stage (non-initial, non-terminal by default)
  - Edit stage names, colors, and icons
  - Reorder stages (updates `sequence` sequentially)
  - Set a stage as default (`is_initial = true` toggle)
  - Configure `metadata` variables per stage
- [ ] Backend Verification: Write `CandidatePipelineSettingsTest` to verify stage CRUD validations, reordering mechanics, default exclusivity constraints, and active application checks.
- [ ] Frontend UI: Build the Candidate Pipeline settings interface (reordering columns via drag-and-drop, name/color/icon editing, metadata form for sorting and visibility details).
- [ ] Frontend Integration: Update the Candidate Kanban board (`pages/hrm/recruitments/candidates.vue`) to load columns dynamically from `workflow_statuses` (`module = 'hrm.application'`) and apply column-level sorting, visibility, and display field preferences from the metadata.

### Phase 12 — Leave & Time Off Settings (Allocations & Calculation)
- [ ] Database Schema: Add `code`, `default_allowance`, `is_paid`, `gender_restriction`, `is_accrued` columns to `leave_types` table. Create `employee_leave_allocations` table mapping employees to leave types per year, tracking allocated, used, and pending days.
- [ ] Database Seeders: Update `TenantDatabaseSeeder` to seed default leave types: 18 Vacation (monthly accrual), 7 Special, 7 Sick, 5 Unpaid, and 90-day Maternity (female only).
- [ ] Backend Services & Logic:
  - Create listener (`EmployeeCreated` / `EmployeeOnboarded` event) to auto-provision leave allocations for the current year based on gender eligibility (e.g. female-only Maternity leave).
  - Refactor `LeaveService::submitRequest()` to calculate leave duration `days` using `WorkScheduleService` intervals: iterating dates in the range, summing daily scheduled hours divided by standard daily hours (default 8.0), and halving the value for morning/afternoon sessions.
  - Refactor balance validation to check available balances against `employee_leave_allocations` (supporting pro-rata monthly calculations for accrued leaves and negative balance bypass settings).
  - Update `SyncLeaveFromApproval` to increment/decrement `pending_days` and `used_days` on the employee leave allocation record.
- [ ] Backend API: Add CRUD controller and FormRequests for Leave Types and manual allocations adjustment endpoints.
- [ ] Backend Verification: Write `LeaveAllocationsTest` covering: auto-allocation on hire (male vs female rules), work schedule decimal duration calculations (including Saturday half-days and morning/afternoon sessions), balance verification, eApprovals locking, and adjustments.
- [ ] Frontend UI:
  - Implement Leave Types configuration grid and edit modal in Settings workspace.
  - Implement Employee Leave Allocations directory showing employee balances per type, with a side drawer for manual balance adjustments.
  - Update Leave Request submit modal to show remaining balance for selected type and live-calculated schedule duration preview.






