# Context: Grid Cards Redesign

The user requested a premium overhaul for all page grid cards based on the implementation in `approvals/forms/index.vue`.
We will apply consistent glass-card parameters, hover borders, scaling background radial glow nodes, and sliding-arrow action affordances.
